apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: public
  namespace: demo
spec:
  rules:
    - http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: checkout
                port:
                  number: 80
